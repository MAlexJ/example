package com.malex.lecture_15_Lambda.binary_operator;

/**
 * Объект BinaryOperator<T> представляет функцию, которая принимает два элемента.
 * Функция выполняет элементами не которую операцию, возвращая результат.
 * При этом метод reduce сохраняет результат и затем опять же применяет к этому результату
 * и следующему элементу в наборе бинарную операцию. Фактически в данном случае мы получим результат,
 * который будет равен: n1 op n2 op n3 op n4 op n5 op n6, где op - это операция (в данном случае умножения),
 * а n1, n2, ... - элементы из потока.
 */
public class BinaryOperatorSample {

    // TODO add new sample -> https://mkyong.com/java8/java-8-binaryoperator-examples/
    public static void main(String[] args) {

    }
}
